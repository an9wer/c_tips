" keys({dict})

let info = {
  \   "author": "an9wer",
  \ }

let b:examples = [
  \   "keys(b:)",
  \   "keys(w:)",
  \   "keys(t:)",
  \   "keys(g:)",
  \   "keys(l:)",
  \   "keys(s:)",
  \   "keys(a:)",
  \   "keys(v:)",
  \   "keys(g:info)",
  \ ]
